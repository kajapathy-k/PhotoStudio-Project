apiVersion: v1
kind: Service
metadata:
  name: photostudio-backend-service
  namespace: photostudio-ns
spec:
  type: LoadBalancer
  selector:
    app: photostudio-backend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007